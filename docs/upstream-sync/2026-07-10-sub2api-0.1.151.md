# 上游同步：sub2api 0.1.151

- **同步日期**：2026-07-10
- **上游终点**：`Wei-Shaw/sub2api` `upstream/main` = `deff3123d`（tag `v0.1.151`）
- **合入目标基线**：`origin/dev` = `860dee559`
- **安全备份**：`backup/dev-before-upstream-sync-20260710-200631` = `860dee559`
- **merge-base(dev, upstream/main)**：`ddb1a210c`
- **代码 merge commit**：`f324d27b7`
- **规模**：从 merge-base 到上游终点共 27 个 commit，其中 18 个非 merge commit；上游净变化 84 个文件、约 2553 行新增/339 行删除。

## 不是只合并 `5260a42a0`

`5260a42a0` 是“用户级 OpenAI Fast/Flex 策略”PR 的 merge commit，只是本轮上游历史中的一个节点，不是本次合并终点。

本次实际执行的是把固定终点 `deff3123ded1d14e51df1fd1286e3d43ed9ec9bd` 合到 dev。Git 会带入该终点可达、但 dev 尚未包含的完整历史，因此同时包含：

- `5260a42a0` 及其 Fast/Flex 策略功能；
- `8a51119e3` 的 Codex identity pairing / 404 修复；
- `deff3123d`（`v0.1.151`）以及此前所有缺失提交。

“固定 commit”是为了让同步范围可复现、避免验证期间上游继续移动，并不代表 cherry-pick 单个 commit。

## 本轮上游功能

- **GPT-5.6 计费完整性**：cache write 价格与 usage 统计、官方价格对齐、显式价格保留、`max` reasoning effort 和模型别名处理。
- **用户级 OpenAI Fast/Flex 策略**：规则可按 user ID、账号类型、模型、service tier 匹配，并覆盖 HTTP/WS 转发路径。
- **Codex 身份头修复**：最终 User-Agent 与 originator 配对，旧版本头提升到当前兼容版本，修复特定 GPT-5.6 请求的 404。
- **OpenAI/Grok 转发加固**：移除带命名空间的 `image_gen` 声明、WS 映射前后模型 reasoning effort 候选补全、Grok 兼容 effort 保留、Windows websocket reset 识别。
- **账号刷新与管理修复**：后台自动刷新纳入 `setup-token`；用户用量 breakdown 的 `request_type` 前后端类型与解析统一。
- **计费/用量兼容**：Cyber blocked request type 放行、Responses/Chat Completions 的 cache read/write usage 兼容字段补全。

## 冲突处理与本地功能保护

合并产生 7 个显式冲突，逐项人工合并，没有使用整文件 `--theirs`：

| 冲突区域 | 处理结果 |
|---|---|
| OAuth 自动刷新账号查询 | 上游新增 `setup-token` 与本地平台集合取并集；最终支持 `oauth/setup-token` 以及 Anthropic、OpenAI、Gemini、Antigravity、KIRO、Grok。 |
| OpenAI forward / passthrough | 保留本地 compact version 对齐和 `recordUpstreamUserAgent`，在记录最终上游 UA 前执行上游 identity pairing，兼得本地审计与 404 修复。 |
| OpenAI response usage | 采纳上游 cache read/write 解析，同时保留本地 `KiroCredits` 传播和 compact stop/capture 调用约定。 |
| Fast/Flex 设置服务 | 保留本地单体 `setting_service.go` 和既有 affiliate/支付等定制，只移植上游 `user_ids` 正数及去重校验；未恢复上游拆出的 `setting_features.go`。 |
| UseKeyModal OpenCode 配置 | 保留本地精简配置与现有模型，增补 GPT-5.6 四个别名；`max` reasoning 只加到 GPT-5.6 系列。 |
| 迁移编号 | 上游 `173_allow_cyber...` 与本地 173–179 冲突，重编号为 `180_allow_cyber_blocked_usage_request_type.sql`，并更新顺序回归测试。 |
| 测试/构造器兼容 | 补齐本地 `ConversationCapturePool` 参数；将旧 Codex version 断言更新为新 identity clamp 语义，不删除本地 UA 记录行为。 |

额外审计确认：

- 本轮没有直接改动 KIRO 专属文件；共享 OpenAI usage 路径中的 `KiroCredits` 仍存在。
- 本地对话归档/UA 历史调用仍位于最终请求发送前的既定位置。
- 本地 migration 173–179 未被覆盖或改名。
- 本地 web chat、provider routing、订阅额度、支付、模型市场、品牌等未处于本轮上游差异范围，未被顺手改动。
- 上游 `v0.1.151` tag 所在提交的 VERSION 文件仍写 0.1.150；本地 merge commit 不会被 tag 解析脚本识别为 exact tag，因此显式将 VERSION 对齐到 0.1.151。

## 验证结果

通过：

- `make check-generate`：Ent/Wire 无漂移。
- `make build`：后端正式构建与前端生产构建通过。
- `go test -tags=unit ./...`：后端全量 unit 测试通过。
- `go test -tags=integration ./...`：后端全量 integration 测试通过。
- `golangci-lint v2.9.0 run --timeout=30m ./...`：0 issues。
- `make test-frontend`：ESLint、Vue typecheck、14 个关键测试文件 / 165 个测试通过。
- 本轮相关前端测试：58/58 通过。

前端全量 Vitest 现状：187 个文件 / 1199 个测试中，184 个文件 / 1194 个测试通过，另有 3 个文件 / 5 个测试失败。已在未合并上游的备份基线 `860dee559` 上 A/B 复现完全相同的 5 个失败，并确认对应源码/测试相对 `origin/dev` 无本轮差异，因此归类为既有测试欠账，不在本次同步中顺手改动：

- `AccountUsageCell.spec.ts`：1 个 stale spy 参数断言；
- `UsageView.spec.ts`：1 个测试缺少目标 tab；
- `KeysView.spec.ts`：3 个过期分组可见性/选择假设。

## 结论与回滚

本轮采用普通 merge commit，完整保留上游父链，不是 cherry-pick。当前评估为可安全进入 dev：本轮新增路径有直接测试覆盖，本地冲突区的定制行为已合并保护，CI 同款构建、单测、集成测试和静态检查通过。

如需回滚或对比，使用备份分支 `backup/dev-before-upstream-sync-20260710-200631`（`860dee559`）；不要删除或重写该分支，直到 dev 验证完成。
