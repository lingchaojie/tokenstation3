# sub2api 0.2.8 后续节点同步（2026-09-25）

状态：实现及本地验证已完成；独立复审、push 和精确 SHA CI 尚未完成。
本归档不代表生产环境已部署。本轮没有修改生产环境或调用真实 provider API。

## 固定坐标与范围

| 字段 | 值 |
|---|---|
| DEV_BASE | `a892514e488bb880987aba7bec3b102d9964fb23` |
| LAST_UPSTREAM / MERGE_BASE | `881f3202694c6bc932446931a30c27d9675178b9` |
| UPSTREAM_PIN | `a3eb7ef302961cba716dc78b39b93b60c467db0e` |
| 上游描述 | `v0.2.8-1-ga3eb7ef30`，不是 exact tag |
| 缺失历史 | 308 commits，其中 178 个非 merge commits；上游范围 554 files |
| 初始显式冲突 | 97 files；自动合并区同样纳入语义审计 |
| 备份分支 | `backup/dev-before-upstream-sync-20260925-a3eb`，指向 DEV_BASE |
| 双父 merge SHA | `6690b9dafbb57fbf9cb39db3d23217a2751be9ed` |
| 最终生产代码 SHA（含首轮复审修复） | `43738668cb99340182c42de0d75e7015a7f615bf` |
| KIRO 参考点 | 保持 `6ba76ea105e065a5aa8dd2b8d2957528ed58935b`，本轮不推进 |

采用完整历史的普通双父 merge，不是单 commit cherry-pick。第一父节点为
DEV_BASE，第二父节点为 UPSTREAM_PIN。后续修复提交保留该 merge 历史，纯文档
提交不改变最终生产代码坐标。
最终 pushed dev SHA、独立审查结论和 CI 链接在完成报告中记录，避免 commit 自指。

获取上游 tags 时发现既有 `v0.1.136` 本地/远端 tag 不同，未强制覆盖；
通过 `git fetch --no-tags ws main` 固定本轮 main，未修改该历史 tag。

## 用户确认及整合决策

- 采用新版推理等级倍率表，保留本站既有默认计费结果：Fable 5.1 max 未显式
  配置时仍为 3 倍；显式配置优先，其他未配置 effort 为 1 倍。
- 纳入 Seedance 原生视频任务、TypeSafe 独立审核引擎、Codex 邀请/credits。
  排除新增返利线下提现登记及其 operation_id migration，保留原有返利功能。
- 新 migration 使用 244（审核引擎元数据）和 245（推理倍率表），历史 SQL
  内容不变。已覆盖旧数据库升级、重复执行、显式倍率与默认兼容。
- 沿用此前 Plugin、Composite、Grok 音频、独立 x_search 等排除决定；
  不因本次合并恢复这些路径。
- 保留本地鉴权、统一 key、计费/额度、KIRO、capture、WebChat 和发布契约。
  对自动合并区的接口拆分、默认值、取消/清理、异步任务归属和重复计费单独审查。
- 简单模式新增 key 窗口扣费保持默认关闭；显式启用后仅更新 key 窗口，
  不引入余额/订阅/平台或账户额度扣减；存储不可用时拒绝而非放行。
- 复审提出 Seedance 按 token 结算但走独立视频入口的策略边界后，用户明确选择
  “沿用独立视频的利润门豁免”。保持现有媒体入口豁免，余额、额度和价格校验
  继续生效；没有改成任务创建时按 token 利润阈值过滤。

详见[批准的风险评估](./2026-09-25-sub2api-0.2.8-risk-assessment.md)及
[执行计划](../superpowers/plans/2026-09-25-sub2api-a3eb-integration.md)。

## 语义审计与回归证据

所有合入区域（含自动合并）按领域记录入口、数据流、错误路径、保留/排除决定
及验证；不是仅凭编译成功接受合并。主要组合缺陷及修复证据见：

- [协调层、设置、审核、路由与计费入口](./evidence/2026-09-25-a3eb/coordinator.md)
- [网关、协议、取消、capture 与模型映射](./evidence/2026-09-25-a3eb/gateway.md)
- [计费、全部变更仓储、迁移与账户域](./evidence/2026-09-25-a3eb/billing.md)
- [前端接口、页面、倍率与生命周期](./evidence/2026-09-25-a3eb/frontend.md)
- [发布工具、部署及数据保留任务](./evidence/2026-09-25-a3eb/deploy-retention.md)

复审要求：测试完成后由全新的独立 reviewer 分区覆盖本轮全部变更及排除项，
逐文件比较本地父节点、上游父节点、公共祖先和结果；任何可执行发现均先修复或
请求用户决策，再重测并交新 reviewer 复审。全部范围明确 SAFE TO PUSH 才允许推送。

首轮在 `1da1df694` 完成了 592 个路径的并集覆盖（最终变更加上游变更，含排除项）：
`review_frontend_release` 216、`review_gateway_protocol` 158、`review_data_core` 218。
前端 reviewer 找到 UsersView 状态筛选后的列表/分页回归及 Seedance 文档错误的
Composite 配置说明；core reviewer 找到新同 key 账号覆盖 OpenCode 已有状态并
组合出无法通过数据库 CAS 的状态对。均补回归验证并修复，见领域证据；首轮不能
作为修复后批准，修复范围需要新的独立全量复审。网关 reviewer 另指出 Seedance
利润门与独立视频豁免的策略边界，用户已确认保持媒体豁免，见上方决策。

修复后全部本地 normal/unit/integration、构建/lint、OpenCode race 和前端全量
测试通过，原始日志坐标见 coordinator.md 的最终验证补充。新的独立 reviewer
将重新覆盖完整非后端与 core 分区；网关分区须核对最终坐标与批准的策略。
归档提交时这些最终批准、push 和 CI 仍待完成，不能把初轮复审等同于推送许可。

## 验证

后端使用 Go 1.27.0，真实容器使用 PostgreSQL 18.1、Redis 8.4；未降级测试镜像。
前端本机 Node 24.15 / pnpm 9.15.9，CI 的 Node 20 结果仍需 exact SHA 门禁。

| 检查 | 结果 |
|---|---|
| `make build`、`make check-generate` | 通过；Ent 无变更，Wire 与输入一致 |
| `go test -timeout=20m ./...` | 通过 |
| `go test -timeout=20m -tags=unit ./...` | 通过 |
| `go test -timeout=20m -tags=integration ./...` | 全部分区通过，见环境说明 |
| `golangci-lint run --timeout=30m`（2.13 / Go 1.27） | 通过，0 issues |
| 关键路径 race、retention race | 通过 |
| 前端 frozen install、lint、typecheck、production build | 通过 |
| 前端全量 Vitest | 首轮 407 files / 3162 tests；复审修复后 407 files / 3165 tests 全通过 |
| release helper、部署脚本 CI 等价检查 | 通过；release helper 14 tests |
| govulncheck | 无可达漏洞；5 个依赖模块公告不影响调用代码 |
| pnpm production audit + 仓库例外检查 | 通过 |

本机临时端口范围仅 60700–61000，初始并行测试出现端口耗尽。纯 Go 测试在
独立 user/network namespace 中扩大自身端口范围，启用 loopback 并移除全部
capabilities，保留普通用户文件权限语义；宿主机 sysctl 未变更。依赖 Docker
发布端口的 upload、middleware、routes、repository、service 包在宿主机串行运行，
其余 integration packages 在隔离网络运行，二者并集覆盖 `go list -tags=integration ./...`。
无法连接 Docker 的早期隔离尝试不计为通过。

基线 A/B：capture readiness 负载超时在原始父节点 50 次、候选 10 次定向重跑通过；
参数校验矩阵的公网 DNS 依赖在父节点与候选同样复现，已仅将两处测试 endpoint
换成无需 DNS 的公网 IP 字面量，未修改生产校验。前端 KeyUsage 卸载后定时器泄漏
在父节点/候选复现后以回归测试修复。非 CI 的 task12 legacy contract 失败经父节点
A/B 确认为既有 Live 字段断言问题，详见部署证据，不作为本轮通过结果。

## 发布与回滚

归档时 push / exact-SHA CI 待完成。推送前再次 fetch 并检查 dev、main、上游漂移，
仅将本地 main 快进到 origin/main；不得把本轮 dev merge 推到 main。
非强制推送 dev/main 后，等待该 dev SHA 的 CI 五项与 Security Scan 两项全部成功。
CI 通过后不再追加仓库提交，最终报告给出准确 SHA 和链接。

原工作区已有文件保持不变，合并在独立 worktree 完成。备份分支保留至用户确认稳定。
代码回滚使用以第一父节点为主线的 `git revert -m 1 <merge SHA>` 并经测试/复审后
普通 push，不 reset/force push 共享分支。数据库回滚需另行确认：不要盲目执行
down migration 或删除新记录，先按计费证据导出/还原旧倍率 JSON，再评估兼容性。
