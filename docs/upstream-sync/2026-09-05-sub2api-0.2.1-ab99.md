# sub2api 0.2.1 integration through ab99d56e9

## Status

All 83 original conflict paths are resolved and staged after decisions D1–D13.
The auto-merged Claude identity conflict (D14) is resolved following explicit
user approval. Final full backend unit/integration, build, lint, security and
generation checks passed; targeted race checks also passed. Frontend full tests
and production build passed on unchanged frontend code. Independent review of
candidate `8ef22af0c29ff3be2432ff025c8e071054af2099` returned NOT SAFE TO PUSH:
Embeddings admission (D15, approved correction now implemented), a 1h-only
pricing regression (D16), and image-download destination validation (D17).
D16 and D17 were subsequently approved and corrected. Corrected backend unit,
integration, targeted race, build, lint and security checks passed; generation
remains consistent. Fresh independent review of correction commit
`7d4f4344765f75c1dfba1434e0ec4eded4e6c130` confirmed a configured 1h-price
display gap (D18). The user explicitly chose to keep model plaza unchanged:
"不用改模型广场 保持这样就好". This display-only limitation is accepted;
no model-plaza implementation or pricing rule is changed.
Nothing has been pushed. A subsequent passing review gate,
final remote-drift checks and exact-SHA CI remain.
The user requires discussion before resolving conflicting functionality or
uncertain implementations.

## Coordinates

- DEV_BASE: `afbe21f3fe5ec19ed38890006e8090cf1a3b5e18`
- LAST_UPSTREAM / MERGE_BASE: `a2fb09260a955676f99cdc92f05469febee82a08`
- UPSTREAM_PIN: `ab99d56e9626e6cd731592dae8553c9758a0efa2`
- Description: `v0.2.1-1-gab99d56e9`
- Backup: `backup/dev-before-upstream-sync-20260905-ab99`
- Integration branch: `merge/upstream-sub2api-20260905-ab99`
- Worktree: `.worktrees/upstream-sub2api-20260905-ab99`
- Origin/dev equalled DEV_BASE at initial fetch.

The upstream range contains 142 commits, including 92 non-merge commits;
353 paths, 17,374 insertions and 1,402 deletions. The complete range is being
merged with `git merge --no-ff --no-commit UPSTREAM_PIN`. During preparation the
working tree had HEAD=DEV_BASE and MERGE_HEAD=UPSTREAM_PIN. The candidate must
retain those ordered merge parents; its SHA is verified after commit creation.
Git reported 79 content conflicts
and four modify/delete conflicts.

The original checkout has pre-existing modifications to `.gitignore` and
`AGENTS.md`; these belong to the user and are outside this integration tree.
The current user-provided AGENTS.md KIRO guidance also applies to this worktree.

## Inherited product boundaries

The September 4 archive remains the reference for local integration decisions:
site-controlled pricing and durable billing deduplication, capture/spool,
unified keys/catalog, WebChat, KIRO direct/relay and local payment/rewards.
The KIRO reference remains `6ba76ea105e065a5aa8dd2b8d2957528ed58935b`.

Prior exclusions are Plugin runtime, Composite product, Grok audio products,
independent `/x_search`, and upstream billing-rate writeback. Changes touching
these surfaces must be traced into shared consumers without restoring excluded
products by accident.

Patch-ID comparison found no fully equivalent non-merge commits, but this does
NOT prove that every upstream capability is absent locally. Local Fable 5.1
and GPT-6 Astra pricing already exist. Local pending-payment reconciliation
already covers WeChat and IkunPay, while upstream now adds Alipay to WeChat.

## Decisions and historical implementation checkpoints

The sections below preserve the evidence available at each checkpoint. Earlier
statements about pending tests are superseded by the later verification records;
the top-level Status section describes the current publication gate.

### D1: Fable 5.1 max reasoning billing

Upstream `billing_service.go` adds a default 3x multiplier for Fable 5.1 when
the final forwarded reasoning effort is `max`. This multiplies the token cost
breakdown, including cache costs. A positive channel multiplier can override
the default. Local billing has no corresponding automatic multiplier.

User decision: “按照上游默认的来”. Adopt upstream's 3x Fable 5.1 model
default for final forwarded `max` reasoning and retain positive channel
overrides. This approval concerns D1; other unresolved domains remain pending.

Implementation in progress: the common Fable price values retain the existing
independent 5.1 card, and upstream's normalized 5.1 matcher is used consistently
with its multiplier. Upstream multiplier plumbing remains. The default-multiplier
regression now includes 5m/1h writes and reads with literal expected totals:
0.01585 Standard, 0.04755 max; max cache writes 0.01575 and reads 0.0003.
This service test cannot run until the remaining package conflicts are resolved;
no passing claim is made for D1 yet.

### D2: Dedicated 1h cache-write price

Question sent: add the upstream independent 1h price while retaining local
Fast tier derivation, explicit-zero semantics, price provenance and missing-price
validation. Absent 1h override preserves old behavior; explicit override applies
only to that TTL bucket. **Approved:** user said “ok 计费默认都按上游的来”.

### D3: Group Fast and Ultrafast rules

Question sent: follow upstream with forced Fast and free Fast both default-off;
force priority before global policy evaluation; free Fast discounts priority/fast
user billing only while recording the observed tier. Ultrafast remains 2x
Standard, without free-Fast discount or channel Fast multiplier. **Approved:**
user said “ok 计费默认都按上游的来”. Billing defaults in this integration follow
upstream, while local accounting, validation, and explicit configured-price
contracts remain part of the semantic integration.

### D4: Migration filenames

Question sent: retain all existing migration names/checksums and renumber the
eight new SQL files to 234–241 in dependency order (request-ID column before its
concurrent index). Test fresh schema and upgrades through local 233 without
production access. **Approved:** user answered “确定 保留” to the proposed
migration renumbering and payment-preservation schemes.

### D5: Pending-payment reconciliation

Question sent: union existing WeChat/IkunPay coverage with upstream's Alipay
addition, preserve the singleton lock, shared 20-order limit, existing idempotent
fulfillment and old method compatibility. **Approved:** user answered
“确定 保留”.

Implemented in the merge candidate: both legacy Wxpay and upstream Payment
method names delegate to the local Provider method; one oldest-first query
selects pending/unexpired WeChat, IkunPay and Alipay orders with a shared limit
of 20. The existing expiry worker singleton lock and cancellation separation
remain. Reconciliation calls the existing notification validation and atomic
PAID/fulfillment path; public unsigned verification remains read-only.

The frontend's existing broader provider predicate already implements upstream's
desktop-Alipay intent and stays intact. The upstream conflict also included
unchanged deep-link test context removed by prior local changes; that obsolete
context was not restored. Provider-parametrized tests cover Alipay, WeChat,
IkunPay and EasyPay verification and the 15-second retry interval.

### D6: Anthropic reasoning policy and over-limit rejection — approved

Question sent: extend group reasoning ceilings/mappings to Anthropic, add the
upstream opt-in deny/403 action, retain default downgrade and local original
requested-effort metadata, without restoring Composite. Existing OpenAI behavior
remains the default; only explicit admin selection would enable rejection.
After explaining that downgrade only applies when an administrator configured a
ceiling and an explicit effort exceeds it, the user confirmed the precise scheme:
integrate the feature, do not proactively set group ceilings, default to downgrade
when a ceiling is configured, and allow explicit deny. User answer: “ok”.

Core service and handler helpers now support Anthropic/OpenAI only. Mapping is
applied before the ceiling; absent efforts are untouched; deny without a ceiling
does not block. Original requested-effort metadata remains separate from final
forwarded effort, with only one set of metadata helpers. The three HTTP entry
points retain local capture and add early policy-error handling, but those large
files still contain unrelated conflicts and are not staged as resolved.

Group/admin field conflicts retain the local KIRO fields, add the upstream
over-limit field, and keep existing empty-ceiling defaults. Full persistence/cache
and forwarding integration remain to be checked after remaining conflicts.
The shared frontend helper now accepts Anthropic and OpenAI without Composite;
the Anthropic source-option list is corrected to exclude minimal, matching both
frontend validation and backend acceptance. This mismatch was reproduced with a
failing test before the one-line source-selection fix.

Added handler regressions cover unlimited/default-downgrade/explicit-deny for
both platforms while preserving original max metadata. They are not yet run:
the backend package still cannot compile while other merge conflicts remain.

### D7: Codex manifest source precedence — approved

Local OpenAI manifests short-circuit upstream discovery when explicit account
model mappings exist. The upstream pinned-account feature would be ineffective
in that branch. Proposed: leave it disabled by default and preserve current local
behavior; when explicitly enabled, prefer the pinned-account source while still
applying local aliases and group display filters. Actual request routing and
permissions remain unchanged. User explicitly approved this scheme: “ok”.

Implemented: the local configured-list fast path runs only while pinned mode is
disabled. Enabled mode uses the pinned fetcher, including its existing optional
scheduler fallback; enabled-but-empty stale config is treated as unavailable,
not silently treated as disabled. Successful pinned bodies pass through the
local alias/display-filter merger before the final-body ETag/304 decision.
The pinned fetcher rechecks current group membership/platform/account status;
its existing bounded concurrent merge uses configuration-order precedence.

Added a handler regression combining explicit account mappings with the toggle,
local alias preservation, removal by group filtering, and ETag invalidation.
Fixed the upstream test helper's constructor call to include the local capture
dependency. Backend execution remains pending other conflicts.

Admin create/update integration retains KIRO normalization and prior local
fields, adds approved reasoning/Fast/manifest fields, and validates pinned
configuration only when explicitly supplied. Authentication snapshot version is
24, combining local authorization data with upstream policy/manifest fields;
older versions are rejected and rebuilt. Shared catalog search-tool metadata
keeps the concrete OpenAI bridge capability without Composite route machinery.

Frontend types and the group editor now expose the edit-only pinned selector,
default disabled/no fallback, preserving local pricing helper extraction and
KIRO form state. The editor updates its reactive configuration in place instead
of letting v-model replace a const reactive binding. Unchanged upstream context
for previously removed Voice/Live/Composite UI was not restored. Locale hints
describe the actual local-first behavior while pinned mode is disabled.

The extracted local group-pricing helpers now carry 1h cache-write prices and
max-effort multipliers alongside Fast/Flex multipliers, including explicit zero.
This auto-merge omission was reproduced by round-trip tests before correction.
Model map/list conflicts retain local Mythos/KIRO behavior and upstream Fable5.1.

### D8: Kimi native Responses and adaptive selection — approved

Local Kimi adaptive Responses requests are translated to Chat Completions.
Upstream adds native Responses for Kimi PAYG/Coding: explicit responses or
adaptive selects the native endpoint, forcing store=false and removing
previous_response_id. Proposed: adopt upstream selection while preserving local
capture/finalization and unchanged ordinary Chat Completions configuration.
User explicitly approved this scheme: “ok”.

The merged forwarding selectors now use native Kimi Responses for adaptive and
explicit Responses configurations. Both HTTP builders apply the upstream
stateless normalization and choose the Responses base URL for adaptive mode;
DeepSeek-only tool adaptation remains scoped to DeepSeek. The local verified
native-Anthropic full-body capture regression is retained. Added Kimi regressions
cover ordinary adaptive/fixed Chat Completions staying on the CC endpoint and
fixed/default CC requests not receiving the Responses-specific body rewrite.
Backend service execution remains pending other conflicts.

The upstream private Chat Completions retry entry carries the tenant-isolation
flag to avoid isolating the prompt-cache key twice. Local capture-attempt setup
and deferred usage/finalization live at that private entry, retaining per-attempt
coverage when AgentIdentity retries internally. There is no second wrapper defer.

Response-header propagation is combined with local complete/incomplete capture,
usage aliases, KIRO credits, typed failover and disconnect handling in the
Bedrock/Antigravity/shared Anthropic converters and both raw-CC fallback
converters. Failed scans still exit before synthetic terminal events. Existing
success-path tests now also assert response-header preservation. Configured-only
request-ID extraction and WS exclusion remain in the common usage helpers.
The generic token-cost path carries final reasoning effort while keeping local
provider-long-context and missing-price/KIRO guards.

### D9: Load-aware channel restrictions and KIRO cooldown recovery — approved

Upstream adds account-mapped model restrictions to all load-aware selection
layers, including routed and sticky accounts, when the channel enables model
restriction with upstream billing-model source. Its empty-candidate branch
reports a channel-restriction error. Local code instead attempts one KIRO
transient-429 cooldown-pool recovery and retries selection before returning
unavailable. Recovery clears one cooldown; suspended/quota states are not cleared.

User approved (“ok”): preserve upstream channel admission checks and
retain the existing one-shot KIRO recovery conditions only for channel-eligible
accounts. Recovered accounts must pass the full selection checks again; if none
qualify, return unavailable. Do not clear a channel-ineligible account's cooldown
or bypass a configured channel restriction.

Implemented in load-aware selection: the upstream route/sticky/candidate gates
remain; before recovery a private copy of the exclusion set also excludes every
channel-restricted account, including accounts skipped earlier for cooldown.
The full account pool is retained so the recovery helper's mixed-platform guards
are not relaxed. The caller's exclusion map is unchanged. Recovery retains the
one-shot context guard and recursively re-runs full selection on success.
Added five regression cases: all restricted, only one admitted, restriction off,
suspended account, and recovery already attempted. Execution is pending package
conflict resolution, not claimed passing.

### D10: Legacy scheduler channel-admission gaps — approved

Call-chain inspection found pre-existing gaps outside upstream's new load-aware
checks. When load awareness is disabled (or no concurrency service exists), the
selector delegates to legacy single/mixed-platform scheduling. Single-platform
non-routed sticky selection lacks an upstream channel check and its adjacent
comment explicitly prefers stickiness. Single/mixed routed candidate loops and
mixed routed stickiness also lack this check. Some other legacy sticky paths
already check it, so the contract currently depends on the selection branch.
Legacy single-platform KIRO recovery also still receives the unfiltered pool.
These regions are unchanged by the pinned upstream diff and have not been
altered under the narrower D9 load-aware resolution.

Approved expansion: enforce the same configured channel
admission rule across legacy sticky/routed/candidate/recovery paths, without
enabling restrictions where the channel has not configured them. Previously
accepted restricted sticky/routed requests would select an admitted account or
return unavailable. This changes an old exception beyond the upstream patch.
After the user requested a detailed explanation with examples and a comparison
to the actual pinned upstream code, the user approved: “ok 那就统一”.

Implemented: the single/mixed legacy selectors evaluate the existing restriction
enable predicate before their routed loops; both routed candidate loops now
check the account-mapped model. Missing checks were added to single non-routed
and mixed routed stickiness; the already-checked sticky branches remain.
Legacy single-platform KIRO recovery receives a private channel-filtered exclusion
set, matching D9 without mutating caller state or relaxing the existing pool guard.
The obsolete comments describing a sticky exception were removed. No restriction
switches, model mappings, pricing values or unrelated account eligibility rules
were changed.

Added 14 single/mixed legacy priority-path cases for ordinary/sticky/routed/
routed-sticky selection, all-blocked rejection and restriction-disabled behavior.
The KIRO recovery matrix now also covers legacy all-blocked, admitted-only,
restriction-disabled, suspended and one-attempt cases. Runtime execution is
pending the remaining service conflicts.

### D11: GLM-5.3 explicit thinking-off normalization — approved

The upstream automatic-merge region introduces NormalizeGLM53AnthropicThinking
and calls it after mapping the model in native Anthropic Messages forwarding.
For exact mapped glm-5.3, disabled/off/none/minimal/low becomes
thinking.type=enabled plus output_config.effort=low; enabled/adaptive/medium/high
becomes enabled/high, and xhigh/max/ultra becomes enabled/max. An explicit output
effort wins over thinking.type. Missing preferences and other models are untouched.
The upstream tests explicitly cover disabled-to-enabled/low, so this is not a
textual merge accident. Local code previously forwarded these native Anthropic
preferences without this new rewrite.

The user explicitly approved this thinking-off-to-low change ("对的") after
the concrete upstream mapping was explained. No claim is made that the provider
rejects disabled mode; only the pinned upstream code/tests have been verified.
The native Messages header reconciliation preserves local response completion
and capture behavior. The normalization matrix additionally asserts that the
returned usage/billing effort equals the forwarded effort, with complete response
and upstream headers preserved. Service test execution remains pending compilation.

### D11 follow-up reconciliation checkpoint

- Native GLM-5.3 tests now assert forwarded and returned effort, response
  completion and upstream headers for the existing 15-case upstream matrix.
- Grok Responses retains local partial-result/capture/finalization and image
  accounting, adds upstream headers, and does not restore excluded standalone
  search counters. The shared Messages failure classifiers remain local;
  proxy attribution already lives in the relocated shared error recorder.
- First-semantic-output timeout retains nil result for safe pre-output retry and
  the full response for capture provenance, with new proxy attribution arguments.
- Four modify/delete files remain deleted as on DEV_BASE: excluded Composite
  tests/Grok audio and the relocated settings implementation. Relevant concrete
  Anthropic policy assertions are preserved in upstream_ab99_reasoning_policy_test.
  The sole new settings delta (ultrafast tier validation) is moved into the active
  local ValidateOpenAIFastPolicySettings before any settings persistence.
- Compiler checks exposed missing imports in the reconciled admin group tests,
  stale excluded Composite arguments in auto-merged Codex metadata tests, and
  Composite fixtures in two Fast-policy tests. Imports/signatures were corrected;
  account eligibility tests now use the retained concrete OpenAI group contract.
- Targeted service commands were attempted for GLM normalization, legacy/channel
  recovery and ultrafast settings validation. They did not execute test bodies:
  initial duplicate declarations from modify/delete artifacts were removed,
  subsequent test compile errors fixed, and the final attempt remains blocked by
  unresolved repository usage-log SQL conflicts. These are build failures, not
  behavioral red/green or passing-test evidence. No test process remains running.

### D12: failed-request session-slot release — approved upstream behavior

The upstream Messages handler introduces immediate release of the Anthropic
OAuth/SetupToken session-limit slot on failed attempts, final failure and failed
count_tokens requests; successful requests and nonnil partial results retain
the existing idle-timeout occupancy. This is separate from concurrent-request
slots, sticky bindings and billing. It only applies when max_sessions is enabled.

Semantic review found that RegisterSession returns only allowed/not-allowed and
refreshes an existing shared session member, while UnregisterSession unconditionally
ZREMoves that member. It cannot distinguish this request's new registration from
a previously served session or another in-flight request sharing the ID. A failed
request can therefore remove a slot that another request still uses. The current
upstream release unit tests verify unconditional deletion/no-op/error tolerance,
not this shared-ownership case. No concurrency reproduction has been run yet;
the risk is inferred from the registration and deletion code paths.

History verification corrected the earlier framing: idle-only release was not an
intentional local customization. DEV_BASE still used the old upstream behavior;
upstream 9be9c0b68c47eb433d886453deecf73c7ed55d68 (2026-09-02) introduced immediate
cleanup to avoid failed requests blocking new sessions for the idle window.
After that explanation, the user explicitly approved adopting upstream cleanup
("是的"), with local capture/billing integration retained. No ownership-aware
redesign is included; the shared-session deletion boundary above is accepted as
the pinned upstream behavior, not asserted solved.

The local result contract also retains capture-only failed responses, so upstream's
`result != nil` alone is not equivalent to an accepted/metered session. The final
handler retains the coupled once-only capture/usage submitter. An error result
retains occupancy only when it is not UpstreamFailed and has billable usage;
capture-only failures still release. Success retains idle-expiry occupancy.
The real handler regression uses setup-token accounts and a stateful external
session-cache substitute: capture-only rejection releases admission for a new
session, success retains it, and partial metered output retains it. All three
assert one capture and the expected usage count. Before the compatibility guard,
the rejection case failed (new session denied); after it, all passed.

### 2026-09-06 conflict-resolution checkpoint

- SQL insert/select/scanner contracts now preserve KIRO credits followed by the
  new upstream request ID, then session ID, native compaction and timestamp.
  There are 63 insert arguments / 64 selected values including ID. All single,
  fallback and both batch CTE column lists align; durable dedup keys are unchanged.
  Literal fixture contracts now include distinct upstream and session IDs, and
  verify KIRO credits are still read correctly.
- Account lite lists keep upstream's omitted group graphs and lazy detail fetch,
  while KIRO enrichment precedes serialization and six KIRO runtime/quota fields
  survive the compact projection. A new serialization test failed on a missing
  quota state, then passed after preserving these fields. It also checks omitted
  group graphs and retained group IDs.
- Account create/edit forms preserve local KIRO defaults/quota/probe eligibility,
  and apply upstream request-ID header configuration without dropping other extra
  fields. Image URL-to-base64 configuration remains upstream default-off. Excluded
  upstream billing-rate writeback is not reintroduced.
- Channel pricing editing retains the local shared conversion helper; independent
  1h cache price (including explicit zero) and max-effort multiplier were added to
  that helper. The targeted regression failed on lost zero, then all helper tests
  passed. User pricing DTOs expose the matching upstream pricing fields without
  changing visibility filters or the empty-interval array contract.
- OpenCode config generation uses existing local helpers and model catalogs,
  adding upstream Astra aliases and Fable 5.1 to concrete Anthropic/Antigravity
  catalogs while retaining local root aliases, Mythos and other existing entries.
  Updated catalog tests preserve the existing default selection assertions.

Verification at this checkpoint (not full completion):

- `go test -tags=unit ./internal/repository -run 'Test(UsageLogStaticInsertShape|PrepareUsageLogInsert_UpstreamRequestIDArgWiring|ScanUsageLog)' -count=1 -timeout=3m`: passed, 0.019s.
- `go test -tags=unit ./internal/service -run 'Test(NativeAnthropicPassthrough|LegacySelectionEnforcesChannelRestrictionAcrossPriorityPaths|LoadAwareKiroRecoveryRespectsChannelAdmission|UpstreamAB99FastSettingsAcceptUltrafast|ReleaseAccountSession|AdminService_UpdateGroup_CodexModelsManifestConfigValidation|GroupBillsOpenAIFastAtStandardRequiresOpenAIAccount|ApplyOpenAIFastPolicyToBody_GroupForce)' -count=1 -timeout=3m`: passed, 0.030s.
- `go test ./internal/handler/dto -run '^TestAccountListItemPreservesKiroRuntimeState$' -count=1`: observed missing-state failure, then passed, 0.015s.
- `go test -tags=unit ./internal/handler -run 'Test(GatewayMessagesSessionReleasePreservesCaptureAndUsage|GatewayForward|GatewayEnsureForwardErrorResponse|UpstreamAB99|UserAvailableChannel|ToUser|BuildPlatformSections)' -count=1 -timeout=3m`: passed, 0.026s, after the new release regression failed at 01:26:19 on capture-only failure retaining occupancy.
- `go test -tags=unit ./internal/repository -run 'Test(UsageLog|PrepareUsageLog|ScanUsageLog)' -count=1 -timeout=3m`: initially failed on stale 62/63-column fixtures; after literal contract updates passed, 0.025s.
- `COREPACK_ENABLE_AUTO_PIN=0 pnpm --dir frontend exec vitest run` with seven paths (channel/types; account/CreateAccountModal, EditAccountModal, UpstreamRequestIdHeaderField; keys/UseKeyModal; modelPlaza/PlazaModelPricingTable; admin/AccountsView.lite): 195 tests passed, 5.77s, start 01:31:11. Earlier run had 193 passing and two stale model-catalog expectations. Cached Browserslist data emitted a warning; dependencies were not changed.

The last frontend/repository checks ran concurrently with independent in-process
fixtures and no shared external database or generated-file writes. Full generator,
build/unit/integration/race/lint/frontend suites and whole-merge independent review
remain required. No commit or push has occurred.

### D13: mapped capability routing versus requested-model admission — approved

Upstream 5e4958c88 changes the Responses, Chat Completions and WS scheduler
arguments from the client model to the channel-mapped forward model. This fixes
account capability selection for public aliases. However, both the pinned
upstream and the merged scheduler pass that same argument to
checkChannelPricingRestriction, which interprets it as the original requested
model when BillingModelSource=requested, and resolves channel mapping again.
The mapping helper explicitly defers restrictions to the scheduler, so it does
not perform an earlier original-model gate that could compensate for this.

The new regression exercises the actual channel mapping and early restriction
helper with exactly the value now passed by the handlers:
public-coding -> gpt-5.6-sol, RestrictModels=true, BillingModelSource=requested.
Two literal cases fail: pricing only public-coding should admit it but the mapped
argument rejects; pricing only gpt-5.6-sol should reject the unconfigured public
alias but this gate admits it. This proves an early admission discrepancy, not
that every downstream billing/authentication check can be bypassed. No full HTTP
reproduction was run. The existing original-model invocation passes both cases.

`go test -tags=unit ./internal/service -run '^TestUpstreamAB99MappedSchedulingPreservesRequestedChannelAdmission$' -count=1 -timeout=3m`
exited 1 with both behavior assertions failing (0.020s, local 01:36:47).
The user approved the compatibility correction with “ok”. The three handler
selection calls now carry a group-scoped original-model context, consumed only
by the early channel restriction check. Capability selection still receives the
mapped model; account-level upstream restrictions and billing are unchanged.
The context is bound only to selection, copies the group ID, and invalid bindings
mask earlier values. No global configuration or pricing is changed.

`go test -tags=unit ./internal/service -run 'Test(UpstreamAB99.*(Admission|MappedScheduling)|OpenAISelectAccountForModelWithExclusions_.*Restriction|IsUpstreamModelRestrictedByChannel)' -count=1 -timeout=3m`
passed (0.025s). Coverage includes requested-model allow/deny, one-step channel
mapping, group isolation, invalid binding, and actual legacy account selection
with mapped model under all three billing-model sources. Existing compact and
passthrough upstream-restriction regressions also ran. Handler compilation passed
in the preceding targeted invocation; no full HTTP D13 reproduction is claimed.

At this checkpoint all 83 index conflicts are resolved and staged, with no
remaining markers. Frontend typecheck passed. Exclusive backend
`go generate ./ent ./cmd/server` passed; after generator exit,
`git diff --exit-code -- backend/ent backend/cmd/server/wire_gen.go backend/cmd/server/wire_gen_test.go`
confirmed no drift from the staged merge result. This is not yet the final CI
`make check-generate` against a committed merge. Full verification and whole-merge
review remain outstanding; D13 is resolved, but publishing remains gated on full
verification and independent review.

### D14: Claude OAuth mimic identity versus cached account fingerprint — approved

Full unit tests expose a semantic conflict in the automatically merged Messages
and count_tokens request builders. Local commit
`7143988432fd80880875972446f4a8122964cd6a` (2026-07-03,
`fix(anthropic): keep cached mimic user agent`) reapplies the account fingerprint
after mimic defaults. Its comment explicitly preserves the learned real CLI
UA/X-Stainless identity. Upstream instead leaves forced server defaults in place,
and its new effectiveBillingUserAgent therefore selects that same server identity
for the body attribution version and recomputed fingerprint suffix.

The merged result combines upstream body-version selection with the local final
header override: a cached test UA `claude-cli/2.9.0` remains on the wire while the
body is constructed for server default `2.1.220`. Both upstream builder cases
fail, and the local cached-UA regression fails on the body attribution. This is
not a pricing-rate conflict despite the billing-header helper's name, and no real
provider request was made to infer rejection behavior.

An isolated rerun reproduced all three assertion failures without the full suite:
`GOMAXPROCS=4 go test -tags=unit ./internal/service -run 'Test(BuildOAuthRequest_BillingMatchesWireUserAgent|GatewayServiceBuildUpstreamRequestMimicUsesCachedFingerprintUserAgent)$' -count=1 -timeout=3m`
exited 1 (0.023s, 01:54:54 local). Log: `claude-identity-conflict.log` below.

The user approved with “ok”: adopt upstream's server-controlled mimic
UA/X-Stainless precedence consistently in both builders, including matching body
attribution and suffix, while retaining local upstream-UA observation/history.
Non-mimic forwarding and unrelated account identity/session behavior would remain
unchanged. The new upstream SUB2API_CLAUDE_CLI_VERSION override is read once at
startup and can explicitly select a supported newer version (not configured by
this merge). Removed only the post-mimic fingerprint reapplication in Messages
and count_tokens. The earlier fingerprint application and later local upstream-UA
recording remain; non-mimic paths are unchanged.

The updated local observation tests plus upstream builder tests first failed
against the old final-header override (0.022s, 01:57:41 local). After the narrow
removal, `go test -tags=unit ./internal/service -run 'Test(BuildOAuthRequest_BillingMatchesWireUserAgent|GatewayServiceBuild.*Mimic|.*RecordUpstreamUserAgent|SyncBillingHeaderVersion)' -count=1 -timeout=3m`
passed (0.030s). The regressions cover actual wire identity, matching body
attribution/suffix, and the identity written into local observation history.

### D15: Embeddings original-model channel admission — approved

Independent reviewer `review_openai` found the Embeddings scheduler call also
changed from reqModel to forwardModel in upstream, but was omitted from D13's
original-model context binding. The mapping call defers restriction enforcement
to selection. Thus requested-model pricing could reject an allowed alias or
admit an unpriced alias; channel-mapped pricing could map twice.

The user approved “ok” to apply the same separation as Responses/CC/WS: only the
selection-call context binds the original group-scoped request model; the
capability argument remains forwardModel. No provider request or billing model
selection is changed. `upstream_ab99_embeddings_admission_test.go` invokes the
real handler, scheduler and forwarding pipeline, replacing only repositories,
concurrency cache and external HTTP boundary. The account supports only the
native embedding name, so reverting capability selection to the alias fails too.

The four-case test covers requested alias allow/deny, one-hop channel-mapped
admission and upstream-model admission. Before correction three cases failed
(0.021s, local 02:57:54); after the single context-argument correction all four
passed (0.019s). Logs `embeddings-admission-red.log` and
`embeddings-admission-green.log` in the D14 log directory. Subsequent validation
passed on the D15 handler/test changes:

- `GOMAXPROCS=3 go test -p 2 -tags=unit ./internal/handler -count=1 -timeout=10m`
  then `CI=true GOMAXPROCS=3 go test -p 2 -tags=integration ./internal/handler -count=1 -timeout=10m`:
  exit 0, 104.608s and 38.576s; combined 02:58:50–03:01:27 local,
  `embeddings-handler-suites.log`.
- `CGO_ENABLED=1 GOMAXPROCS=2 go test -race -p 2 -tags=unit ./internal/handler -run '^TestUpstreamAB99EmbeddingsPreservesChannelAdmission$' -count=1 -timeout=3m`:
  exit 0, 1.072s, `embeddings-admission-race.log`.
- golangci-lint v2.13.0 with GOMAXPROCS=2, concurrency=2, timeout=15m:
  exit 0, 0 issues, `embeddings-lint.log`.

The full handler suites ran sequentially; targeted race, temporary billing
diagnostic and lint were independent consumers with capped concurrency, no
generation and no shared DB/provider resources. D16/D17 still block publication;
these results do not imply the entire candidate is ready to push.

### D16: 1h-only override versus generic fallback cache price — approved correction

Independent reviewer `review_gateway_billing` found a newly imported upstream
defect: an independent 1h override enables SupportsCacheBreakdown on built-in
fallback models whose 5m field is absent. Aggregate-only cache usage passes
validation using the generic rate but is then priced using the zero 5m field.
This is not a deliberate explicit-zero price and is not unchanged local debt.

The main agent reproduced it using a temporary Go overlay, without changing
repository billing implementation or test files. For built-in claude-sonnet-4,
1,000 aggregate cache-write tokens cost USD 0.00375 without an override and
USD 0 after adding only a 1h rate of 6e-6. Literal expected-cost assertion failed
(0.018s): `GOMAXPROCS=3 go test -overlay=/tmp/sub2api-ab99-d14-Wj695I/cache-1h-repro-overlay.json -p 2 -tags=unit ./internal/service -run '^TestReviewOneHourOverridePreservesAggregateCachePrice$' -count=1 -v -timeout=3m`.
Log `cache-1h-repro.log`. The user then explicitly requested “修复”: correct the
unintended zero without changing upstream default rates or explicitly free prices.

The correction tracks effective 5m price presence through Standard/Priority
selection. Only aggregate usage with no TTL details and no configured effective
5m price falls back to the already validated generic rate. It runs before existing
long-context scaling; explicit TTL usage still requires its own configured price.
No shared pricing object, source/provenance labels, default rate, 1h override,
schema or account-statistics rule is changed.

New tests use the actual generic-only built-in fallback across legacy, channel,
group, matching and nonmatching interval paths, paid/free 1h overrides, Standard
and Priority. Additional presence cases retain configured paid/free 5m, free
Priority 5m, free generic, missing-price rejection, and long-context multiplication.
The original 20-case aggregate matrix and missing-5m case failed before the fix
(0.022s, `cache-1h-fix-red.log`); the added long-context case also failed before
production edits (0.019s, `cache-1h-long-context-red.log`). After correction:
`GOMAXPROCS=3 go test -p 2 -tags=unit ./internal/service -run '^Test(UpstreamAB99.*Cache.*|UpstreamAB99OneHour.*|CalculateCost_SupportsCacheBreakdown|ComputeCacheCreationCost.*)$' -count=1 -timeout=3m`
passed (0.023s, `cache-1h-fix-green.log`). Full-unit, billing-race, build and lint
reruns started at 03:17:27–03:17:29 local; results pending at this checkpoint.

`CGO_ENABLED=1 GOMAXPROCS=2 go test -race -p 2 -tags=unit ./internal/service -run '^Test(UpstreamAB99.*Cache.*|UpstreamAB99OneHour.*|.*Billing.*|CalculateCost.*|ComputeCacheCreationCost.*)$' -count=1 -timeout=10m`
passed (24.541s; complete 03:19:51), log `cache-1h-race.log`. Code/test tree was
recorded as `ef53e827192f7518cca1ab8643f93baec0826ce9` after staging D16; later
edits at this checkpoint are archive-only. No generation ran concurrently.

Final D16 verification results:

- `GOMAXPROCS=3 go test -p 3 -tags=unit -count=1 -timeout=20m ./...`:
  exit 0, 03:17:27–03:22:27 local; service 225.496s, handler 106.254s.
  Log `cache-1h-full-unit.log`.
- `GOMAXPROCS=2 make build` followed by golangci-lint v2.13.0 with
  concurrency=2 and timeout=15m: exit 0, lint `0 issues.`,
  03:17:29–03:21:07; log `cache-1h-build-lint.log`.
- Full integration has not yet been rerun after D16. Fresh independent review,
  the D17 decision, remote-drift checks, push and exact-SHA CI remain outstanding.

### D17: image URL backfill destination validation — approved correction

Both OpenAI and data/frontend reviewers traced the new opt-in image URL backfill
through a public-address DNS precheck into a separate ordinary direct/proxy
resolution. Checked addresses are not bound to the connection. Split DNS or DNS
rebinding can therefore permit private-destination GETs despite the public-only
marker; MIME checks happen after the GET. The helper limitation is inherited,
but this upstream-response-controlled URL download path is newly introduced.
No live exploit/provider request was performed. The feature remains default-off;
no routing/security correction or broader proxy-policy choice has been approved.

The main agent subsequently reproduced the proxy boundary with isolated local
DNS, HTTP proxy and private-origin fixtures, using a temporary Go overlay only.
The fixture DNS supplies a public test address; the local proxy emulates a
different DNS view by routing the original hostname to its local private origin.
The real `NewHTTPUpstream(...).Do` public-only path returned success and the
private fixture received one GET (expected zero). Command:
`GOMAXPROCS=2 go test -overlay=/tmp/sub2api-ab99-d14-Wj695I/public-host-proxy-repro-overlay.json -p 1 -tags=unit ./internal/repository -run '^TestReviewPublicHostsOnlyProxyMustNotReachPrivateOrigin$' -count=1 -v -timeout=2m`.
Exit 1, 0.023s; log `public-host-proxy-repro.log`. No external DNS, provider,
production target, account credential or proxy was used. The overlay does not
change repository code/tests and runs in a separate process from other suites.

The user subsequently approved ("ok") preserving the account proxy, pinning
downloads to validated public IPs, and retaining the original URL on unsafe or
unsupported download paths. This is a bounded correction to the existing opt-in
download flow; the default remains off and ordinary API transports are unchanged.

Implementation isolates the marked request's transport for every redirect hop.
It validates the complete DNS answer at dial time and passes only numeric targets
to the direct/SOCKS dialer. HTTP/HTTPS proxies use authenticated CONNECT to the
validated numeric target for both destination schemes; unsupported tunnels fail
closed without proxy bypass. The original URL/Host and TLS verification identity
are preserved. Cached API connections and custom TLS dialers are not reused for
these downloads. This assumes the configured proxy obeys its CONNECT/SOCKS
destination; it does not claim control over a malicious proxy's network routing.

The repository regression failed before production edits (private-origin GET
count 1 instead of 0, `public-host-red.log`). The initial matrix also failed on
domain-valued direct dial and non-IP proxy targets (`public-host-matrix-red.log`).
After the correction, that matrix plus existing public-host guards passed
(0.029s, `public-host-green.log`). Expanded real local socket tests passed
(0.044s, `public-host-expanded.log`), covering origin Host/SNI/certificate checks,
HTTP/HTTPS proxies and destinations, SOCKS numeric targets, mixed/private DNS,
DNS changes between precheck and dial, relative/private redirects and CONNECT
cancellation. Test DNS is local and restored after each nonparallel test; no live
provider, account credential or production endpoint was accessed.

The new service regression checks that a rejected safe download preserves the
entire original response, including URL and usage, with no proxy-bypassing retry.
Full final verification and fresh whole-merge independent review are pending.

D17 final-verification checkpoint (September 6, local time):

- Proxy CONNECT response parsing retains net/http's default 10 MiB header
  budget; the limit is lifted after CONNECT so it cannot truncate image data.
  An 11 MiB header regression failed before this bound (0.067s,
  `public-host-header-red.log`). The final repository/service targeted matrix
  passed (0.106s / 0.068s, `public-host-final-targeted.log`).
- Exclusive `GOMAXPROCS=3 make check-generate`: exit 0,
  12:59:48–13:00:14, `public-host-final-generate.log`; no ent/Wire drift.
- Final consumer batch started at 13:00:38 against staged tree
  `2a8baf4bec2fbef45e58e1293cc9a140d3d90c70`: full unit (GOMAXPROCS=3, -p 3),
  targeted repository/service/handler race (GOMAXPROCS=2, -p 2), and backend
  build then lint (GOMAXPROCS=2, lint concurrency=2). No generator, integration
  suite or frontend build ran concurrently. Each command has a separate log;
  tests use local ephemeral socket fixtures and do not share provider/DB state.
- Targeted race: exit 0, repository 1.605s, service 2.366s, handler 1.085s,
  complete 13:03:06, `public-host-race.log`.
- Backend build passed. Initial lint failed on 15 test-style/checked-error
  issues and the intentional inspection of legacy DialTLS. Test cleanup/type
  assertions/constants were corrected; legacy custom TLS rejection remains,
  with a narrowly documented deprecation suppression (no new deprecated dial).
  Production runtime code is unchanged by these lint edits.
- Lint rerun: exit 0, `0 issues.`, 13:04:14–13:04:36,
  `public-host-lint-final.log`. Repository targeted race after test edits:
  exit 0, 1.552s, complete 13:04:34,
  `public-host-repository-race-final.log`. Full repository unit is rerun after
  the earlier full-suite repository process completed; this is not an
  overlapping duplicate repository invocation.
- Frontend, deployment, workflows, go.mod and go.sum are unchanged since
  candidate `8ef22af0`; the earlier full frontend/build/deployment checks
  therefore remain applicable. Full integration and govulncheck are rerun for
  the corrected backend. The post-lint staged tree before archive-only edits
  is `bdc842cf3bed3bcf23fd0b329c5becf4cb1ecd32`.
- `GOMAXPROCS=3 go test -p 3 -tags=unit -count=1 -timeout=20m ./...`:
  exit 0, 13:00:38–13:05:08; handler 106.341s, repository 3.998s,
  service 225.903s, `public-host-full-unit.log`. The repository unit suite
  after lint-only test edits also exited 0 (3.283s, 13:04:56–13:05:11,
  `public-host-repository-unit-final.log`).
- `GOMAXPROCS=2 /tmp/sub2api-ab99-d14-Wj695I/govulncheck ./...`:
  exit 0, 13:04:54–13:05:08, `public-host-govulncheck.log`;
  zero reachable/imported-package vulnerabilities, six required-module
  vulnerabilities not reached by this code.
- Final `GOMAXPROCS=2 make build`: exit 0, 13:05:41–13:05:52,
  `public-host-build-final.log`. The concurrently started full integration
  suite uses GOMAXPROCS=3 and -p 1; no other test suite or generator overlaps.
- `GOMAXPROCS=3 go test -p 1 -tags=integration -count=1 -timeout=20m ./...`:
  exit 0, 13:05:40–13:11:19, `public-host-full-integration.log`;
  handler 37.897s, repository 24.395s, service 150.307s, capture sidecar 1.284s.
  No ignored failure, weakened timeout assertion, provider request or production
  access. The complete review union now has 392 paths: OpenAI 103,
  gateway/billing 147, data/frontend 142, with no overlap or omitted path.

### D18: configured 1h price omitted from model plaza — accepted unchanged

Fresh gateway/billing review traced the normal, dependency-injected
`ModelPlazaService.ListGroups` path through `fillDisplayPricing` into
`plazaPricingFromSchedule` and `plazaIntervalsFromTiers`. `ContextPricingTier`
contains no independent 1h field; the effective pricing projection therefore
erases configured `CacheWrite1hPrice`. The frontend's existing conditional 1h
display receives nil. This affects the effective token-price display, not actual
billing or admin price persistence/edit round-trips. UPSTREAM_PIN has the same
omission; DEV_BASE did not have the new independent channel/group/interval 1h
price field. The pre-existing official-tier display limitation is distinguished
from this newly introduced configured-price omission.

Reviewer and coordinator separately ran the frozen four-case temporary overlay
`/tmp/sub2api-ab99-d14-Wj695I/review2-plaza-overlay.json`, backed by
`review2-plaza-1h_test.go`. All flat paid, flat explicitly free, interval paid
and group paid cases first passed real billing assertions for 1,000 reported
1h-cache tokens, then failed the literal expectation of a nonnil effective plaza
1h price. The coordinator command was
`GOMAXPROCS=2 go test -overlay=/tmp/sub2api-ab99-d14-Wj695I/review2-plaza-overlay.json -p 2 -tags=unit ./internal/service -run '^TestReview2PlazaRetainsConfiguredOneHourPrice$' -count=1 -timeout=3m`:
exit 1, 0.021s, 13:22:08–13:22:13 local,
`review2-plaza-main-repro.log`. The overlay does not change repository tests or
implementation, and no provider or production endpoint was used.

Round 1 recorded a display observation without this end-to-end configured-price
proof; the fresh reproduction establishes an actionable new-feature gap. The
user did not approve accepting this limitation. Proposed decision: carry the
effective 1h price through the model-plaza schedule/projection, including explicit
zero, group and interval prices, without changing any actual billing rule or
configured/default rate. At that checkpoint, user confirmation was pending.

Final user decision: "不用改模型广场 保持这样就好". Keep the existing
upstream model-plaza behavior, including the described configured-1h display
omission. Do not implement the proposed display correction. The demonstrated
actual billing and admin persistence behavior stays intact; this acceptance
does not authorize unrelated omissions or broader pricing changes. Only this
archive changes after the reviewed code candidate.

Post-decision check:
`GOMAXPROCS=2 go test -p 2 -tags=unit ./internal/service -run '^Test.*(Plaza|ContextPricingSchedule|UpstreamAB99.*Cache|UpstreamAB99OneHour).*' -count=1 -timeout=5m`
exited 0 (0.060s, 13:35:43–13:35:50 local), log
`d18-accepted-targeted.log`. Only this archive differs from reviewed code SHA
`7d4f4344765f75c1dfba1434e0ec4eded4e6c130`; full-suite/build/race/security
evidence remains tied to unchanged runtime/test files. A preparatory fetch of
origin and upstream found dev/main/pinned upstream-main unchanged; only upstream's
unrelated cla-signatures branch advanced. Final post-review fetch remains required.

### Independent review round 2 — publication held for D18

Reviewed HEAD `7d4f4344765f75c1dfba1434e0ec4eded4e6c130`, tree
`0d7974c1d77657932d16147c57ad34a91cb933c8`; its parent is the original two-parent
merge `8ef22af0c29ff3be2432ff025c8e071054af2099`. The final DEV_BASE delta is
377 paths, 20,086 insertions and 1,524 deletions; the broader upstream/final
review union is 392 paths. No runtime code changed during review.

- `review2_openai`: SAFE TO PUSH for its 103/103 assigned paths; independent
  focused handler/service/WS checks passed. Report `review2-openai-report.md`.
- `review2_data_frontend`: SAFE TO PUSH for its 142/142 assigned paths,
  explicitly preserving the cross-partition D18 hold. Independent backend
  contracts and 69 frontend tests passed. Report `review2-data_frontend-report.md`.
- `review2_gateway_billing`: NOT SAFE TO PUSH pending D18; 147/147 paths
  covered with one P2 configured-1h effective display gap, no other finding.
  Report `review2-gateway_billing-report.md`.

The coordinator checked all written report entries against their inventories:
103 + 147 + 142 = 392 unique paths, zero missing/extra/duplicate entries. An
initial coordinator parser recognized bullet/table entries but not the billing
report's numbered entries; correcting the parser verified the full coverage.
Final review reports describe the clean frozen code candidate; the coordinator
then made only these archive updates to record D18 and the review outcomes.

Reports and inventories are under `/tmp/sub2api-ab99-d14-Wj695I`. Partition
passes do not authorize whole-candidate publication. No final fetch, push or
new CI run has occurred; the original worktree's user-owned edits remain intact.

### Independent review round 1 — all partitions NOT SAFE TO PUSH

Three fresh read-only reviewers covered the 389-path union, with complete
inventory/report equality: `review_openai` 102, `review_gateway_billing` 147,
`review_data_frontend` 140. Reports are `review-openai-report.md`,
`review-gateway_billing-report.md`, and `review-data_frontend-report.md` in
`/tmp/sub2api-ab99-d14-Wj695I`. The duplicate image finding is counted once.
The reviewed candidate tree was `7c36a8dbd2ddc1e581551b368e17fb949c8181dc`,
with DEV_BASE/UPSTREAM_PIN as ordered parents. No reviewer modified the checkout.

The data reviewer independently verified all 287 historical SQL files unchanged,
all 64 Group runtime field indices aligned, selected backend contracts passing,
and 69 frontend tests passing. Additional inherited limitations were distinguished
from blockers: pre-admission failed sessions still use prior idle-expiry behavior;
the model-plaza schedule projection omits 1h price display; some Grok discovery
tests were removed upstream. These notes do not authorize new behavior changes.

### 2026-09-06 full-verification checkpoint (around 01:46–01:54 local)

Raw logs for this checkpoint are in `/tmp/sub2api-ab99-validation-oHcvH2`.
Generation was completed exclusively at the preceding checkpoint. Consumers were
run without concurrent generation; Go used its safe shared build cache, backend
build wrote bin/server, frontend checks used jsdom/mocks and frontend build wrote
the ignored backend/internal/web/dist directory. Deployment tests used their
temporary mock container CLI (no production containers). Backend unit concurrency
was capped with GOMAXPROCS=4 and -p 4; lint used concurrency 3. Frontend CI, full
suite and production build ran sequentially. No external DB/service was used for
these unit regressions, and no duplicate full-suite invocation was launched.

- `make build` in backend: exit 0 (Go 1.27.0, CGO disabled).
- `COREPACK_ENABLE_AUTO_PIN=0 make test-frontend`: exit 0; lint and typecheck pass,
  33 critical files / 353 tests pass (01:47:40, 10.74s), 5 WebChat files / 75 tests
  pass (01:47:51, 3.38s). Some fixture/router warnings are present in the log.
- `COREPACK_ENABLE_AUTO_PIN=0 pnpm --dir frontend run test:run`: exit 0,
  330 files / 2561 tests pass (01:48:22, 56.55s).
- `COREPACK_ENABLE_AUTO_PIN=0 make build-frontend`: exit 0; Vite 50.14s,
  with stale Browserslist and bundle-size/dynamic-import warnings.
- CI deployment commands (bash syntax, Apple mock lifecycle, compose security,
  gateway env, runtime resources, Caddy cache): all exit 0 on this Linux host.
  The macOS CI job itself has not run yet.
- golangci-lint v2.13.0 installed into the temporary log directory; invocation
  `GOMAXPROCS=3 .../golangci-lint run --concurrency=3 --timeout=30m`: exit 0,
  `0 issues.` This is not independent review or a passing unit suite.
- `GOMAXPROCS=4 go test -p 4 -count=1 -timeout=20m -tags=unit ./...`: exit 1.
  Repository 3.388s failed two obsolete SQL fixture cases (new 1h-price column);
  service 228.620s failed the D14 identity cases plus three new Fast-policy tests
  whose mock response contained only `[DONE]` without terminal usage. Other
  packages passed, including handler 108.212s. No baseline-debt claim is made.
- SQL fixture updated with separate literal image-input and 1h-cache prices;
  Fast fixtures now provide a completed response with usage, preserving local
  missing-usage failure semantics instead of weakening production checks.
  `GOMAXPROCS=4 go test -p 2 -tags=unit ./internal/service ./internal/repository -run 'Test(OpenAIGatewayService_OAuth(Passthrough|Legacy)_GroupForce|AccountStatsModelPricingImageInputPriceRoundTrip)' -count=1 -timeout=3m`:
  exit 0, service 0.024s / repository 0.018s. These are fixture-only changes.

Additional main-agent audit this checkpoint traced mapped scheduler admission,
WS replay shallow sharing/immutable payload ownership and invalid-ciphertext
lineage (HTTP/WS/HTTP bridge), image URL backfill destination/redirect checks,
tool-discovery promotion through CC lowering, account lite DTO/menu consumers,
model metadata intersection, header attribution, and Ops queue bounds. This does
not replace the required full independent post-test review. Integration, race,
security scan, final full-unit rerun, branch-drift checks, review, commit, push and
exact-SHA CI completion remain outstanding.

### 2026-09-06 post-D14 verification checkpoint

Raw logs: `/tmp/sub2api-ab99-d14-Wj695I`. Unit and integration started from
index tree `a7dbf349eb6e1e1de2cdb96766d7b7db45d3c4be`, with no concurrent
generation. Unit used GOMAXPROCS=4/-p 4; integration used CI=true,
GOMAXPROCS=3/-p 3 and the repository's isolated testcontainer/temp fixtures.
Build and lint ran with concurrency capped at 2. All four processes completed
before the serial integration rerun. No production resources were accessed.

- `GOMAXPROCS=4 go test -p 4 -count=1 -timeout=20m -tags=unit ./...`:
  exit 0, 01:59:13–02:03:59 local; service 225.290s, repository 3.726s.
- `GOMAXPROCS=2 make build`: exit 0, `backend-build.log`.
- `GOMAXPROCS=2 /tmp/sub2api-ab99-validation-oHcvH2/golangci-lint run --concurrency=2 --timeout=30m`:
  exit 0, 0 issues, `backend-lint.log`.
- First full integration command, `CI=true GOMAXPROCS=3 go test -p 3 -count=1 -timeout=20m -tags=integration ./...`:
  exit 1, 01:59:14–02:03:43. The two migration sequence expectations stopped at
  233 while the actual upgrade applied approved migrations 234–241. Updated only
  the two literal test filename lists; historical SQL and production code were
  not modified. Capture sidecar's cancellation-probe test also timed out on its
  real 100ms deadline. This failure has not been classified as baseline debt.
- A/B command in independent DEV_BASE and merged worktrees:
  `CI=true GOMAXPROCS=3 go test -p 3 -count=30 -tags=integration ./internal/capture/sidecar -run '^TestLiveContextCancellationErrorsFromProbeAreRejected$' -timeout=3m`.
  Both exit 0 (baseline 0.870s, merged 0.865s). The package has no diff against
  DEV_BASE. This does not establish a baseline failure or prove a load-related
  cause; no timeout, assertion or production implementation was changed.
- Serial full integration rerun started at 02:06:21, after staging the two
  migration fixture updates (tree `dc9427644234ca9062edf90e7bdb4195c0f900be`).
  Exit 0 at 02:09:33, service 151.256s, repository 25.779s, capture sidecar
  1.584s; log `backend-integration-serial.log`. The earlier probe timeout did
  not recur in this complete run; its cause remains unconfirmed.
- `COREPACK_ENABLE_AUTO_PIN=0 pnpm --dir frontend audit --prod --audit-level=high --json`
  produced `pnpm-audit.json`. The first exception checker invocation failed
  because `python` was absent; the same checker with `python3` exited 0:
  `python3 tools/check_pnpm_audit_exceptions.py --audit /tmp/sub2api-ab99-d14-Wj695I/pnpm-audit.json --exceptions .github/audit-exceptions.yml`.
- CI-equivalent govulncheck installed in the temporary log directory and run as
  `GOMAXPROCS=3 /tmp/sub2api-ab99-d14-Wj695I/govulncheck ./...`: exit 0,
  no reachable vulnerabilities, no imported-package vulnerabilities, six
  required-module vulnerabilities not reached by the application.
- Final exclusive `GOMAXPROCS=3 make check-generate` in backend: exit 0,
  02:09:55–02:10:21; ent and Wire regeneration caused no working-tree/index
  drift. Log `generate-final.log`.
- Targeted race command started alone at 02:10:46 (no concurrent generator,
  build, lint or suite):
  `CGO_ENABLED=1 GOMAXPROCS=3 go test -race -p 2 -tags=unit -count=1 -timeout=15m ./internal/service ./internal/handler ./internal/repository ./internal/service/openai_ws_v2 -run 'Test(UpstreamAB99|GatewayMessagesSessionRelease|LegacySelectionEnforcesChannel|LoadAwareKiroRecovery|ReleaseAccountSession|OpenAIWSStateStore|CollectOpenAIEncrypted|StripOpenAIInvalid|PricingHotReload|PricingScheduler|PricingCustomFiles|BuildOAuthRequest_BillingMatchesWire|GatewayServiceBuild.*Mimic|Relay_|.*InvalidEncryptedContentLineage|OpenAIWSReplay|.*Billing.*|.*CacheWrite1h.*|.*SessionLimit.*|ReconcilePending.*)'`.
  Exit 0 at 02:14:04: service 25.262s, handler 24.971s, repository 1.149s,
  WS relay 5.362s. This is a high-risk-path race check, not a full-repository race
  run. Log `backend-race-targeted.log`.
- Preliminary read-only `git ls-remote` of upstream/main and origin/dev/main
  still returned the recorded pins during verification. Required final fetch
  and drift checks will be repeated after independent review, before publication.

Independent review coverage is the union of upstream-range and final-candidate
paths, 389 entries: OpenAI service/handler (102), other service/handler plus
domain/pkg/util (147), and data/generated/schema/frontend/docs/deploy (140).
Review includes unchanged/deleted local exclusions touched by the upstream range,
not just the 374 paths in the staged candidate. Each reviewer must return its
complete per-path coverage and explicit verdict before publication is considered.

## Migration and payment checkpoint

New SQL filenames are now 234 request-ID column, 235 1h cache pricing,
236 force Fast, 237 reasoning over-limit action, 238 concurrent request-ID index,
239 free Fast, 240 max-effort multiplier, and 241 Codex manifest configuration.
The runner's nontransactional-index constant and migration tests reference the
new names. The request-ID column comment now matches upstream runtime behavior
(unset header means no recording). No existing SQL file was modified according
to `git diff --cached HEAD --name-status -- backend/migrations`.

Added a migration layout/column-before-index regression. Extended schema
integration assertions for the new group defaults, four 1h price columns,
max-effort multiplier and request-ID column/index. Preserved local IkunPay paid
recovery coverage, upstream Alipay pending-query coverage, and added a mixed
24-order regression proving only the oldest 20 are queried across providers.
Backend service/schema integration tests remain unexecuted because other package
conflicts prevent compiling this candidate.

## Billing conflict resolutions awaiting tests

| Path | Combined behavior |
|---|---|
| `backend/internal/service/billing_service.go` | Upstream max-effort multiplier and 1h cache override; local price-presence checks and tier ratios. One Astra fallback card with Priority fields and strict long-context threshold. |
| `backend/internal/service/model_pricing_resolver.go` | Flat/interval 1h overrides update local Standard/Priority values and provenance; 1h-only price cards are recognized, including explicit zero. |
| `backend/internal/service/pricing_service.go` | Upstream Astra aliases and custom-file hot reload; local layered price parsing preserved; duplicate Astra static variable removed. |
| `backend/internal/service/openai_gateway_usage.go` | Free Fast changes customer ActualCost while retaining tier TotalCost; preflight also checks the Standard-price path; max multiplier applied with the local long-context gate. |
| `backend/internal/service/account_stats_pricing.go` | Carry final reasoning into custom/default/provider-long-context cost branches; retain local image/video units, interval inheritance and explicit-zero behavior; add split 1h cache pricing. |
| `backend/internal/repository/channel_repo_account_stats_pricing.go` | Read/write the new 1h field alongside local image-input price, with matching SQL placeholders and scan order. |

Added `upstream_ab99_cache_pricing_test.go` covers flat/interval Priority cache
prices, explicit-free 1h, provenance, 1h-only configured cards, and missing 5m
prices. Extended the Fable max-effort regression to assert both cache TTLs and
cache reads. All these service tests remain unexecuted while other package
conflicts exist. `gofmt` parsed the six resolved Go files successfully;
`git diff --check` for those paths exited 0.

### Other domains still under investigation

- Group forced Fast, free Fast and Ultrafast billing/override precedence.
- Cache-write 1h pricing and local explicit-zero/missing-price validation.
- Payment reconciliation: run service regressions and review edge cases after
  remaining package conflicts are resolved; D5 already approves the combination.
- Migration fresh-schema/upgrade execution remains to be verified; filename
  renumbering is implemented under D4, preserving existing checksums.
- Codex pinned-account manifests versus the local unified display-only catalog.
- Request-ID propagation and forwarding completion versus local capture and
  billing finalization.

These entries are remaining investigation/verification topics. D1–D10 record the
approved choices, including D11.

## Execution checkpoints

- [x] Refresh origin/upstream and fix coordinates.
- [x] Preserve original dirty checkout and establish backup/worktree.
- [x] Run focused pre-merge baseline.
- [x] Start the complete two-parent merge and enumerate unmerged paths.
- [ ] Resolve product decisions with the user and record their answers.
- [ ] Complete a path-by-path review of conflicts and automatic merges.
- [ ] Apply approved semantic combinations and targeted regression coverage.
- [ ] Verify migration upgrade/fresh-schema behavior and generation consistency.
- [ ] Run backend build/unit/integration/race/lint and frontend checks/build,
      plus repository deployment checks with isolated resources.
- [ ] Obtain the sync skill's independent review of the complete merge surface.
- [ ] Recheck remote drift, publish within authorized scope and verify CI.

## Verification so far

At unmodified DEV_BASE, from `backend/`:

```sh
go test -tags=unit ./internal/upstreamcontract ./migrations ./internal/service \
  -run 'Test(.*Fable51.*|.*OpenAIFast.*|.*ReasoningEffort.*|.*UpstreamContract.*|.*Migration.*)$' \
  -count=1 -timeout=5m
```

Exit 0: upstreamcontract 0.017s, migrations 0.006s, service 0.294s.
This is a focused baseline only; the merged tree has not been built or tested.

After merge, the independent package command
`go test -tags=unit ./internal/pkg/claude ./internal/util/urlvalidator -count=1 -timeout=3m`
passed (exit 0, 0.003s and 0.002s). This validates only those two packages,
not the conflicted service/handler tree or end-to-end image downloads.

Automatic-merge findings corrected in the staged candidate: duplicate
`gpt-6-astra` card initialization in `billing_service.go` and duplicate
`openAIGPT6AstraFallbackPricing` variable in `pricing_service.go`. The remaining
cards retain Standard/Priority prices and the strict >272000 threshold.

After D4/D5 changes:

- `go test -tags=unit ./migrations -count=1 -timeout=3m`: exit 0, 0.006s,
  including the new layout regression. This is embedded-SQL contract validation,
  not evidence of a database upgrade execution.
- `pnpm exec vitest run src/components/payment/__tests__/PaymentStatusPanel.spec.ts`
  from `frontend/`: exit 0, 9 tests, 1.84s total (start 19:18:12 local).
- `gofmt` parsed the resolved payment and schema-test Go files;
  staged `git diff --check` for this checkpoint exited 0.
- Frontend dependencies installed offline with the frozen lockfile. Corepack's
  incidental packageManager addition was removed; package.json remains unchanged.
- Original checkout still has only the pre-existing `.gitignore` and `AGENTS.md`
  edits. HEAD/MERGE_HEAD remain pinned; no commit or push has occurred.

After D6 core changes:

- `COREPACK_ENABLE_AUTO_PIN=0 pnpm exec vitest run src/views/admin/__tests__/groupsReasoningEffort.spec.ts`
  reproduced the Anthropic source-option mismatch: 1 failed / 17 passed, extra
  minimal option (20:52:59 local).
- After the fix, `COREPACK_ENABLE_AUTO_PIN=0 pnpm exec vitest run src/views/admin/__tests__/groupsReasoningEffort.spec.ts src/components/admin/group/__tests__/ReasoningEffortPolicyFields.spec.ts`
  exited 0: 21 tests, 1.65s (20:53:36 local). Non-failing warning: stale cached
  Browserslist dataset; no dependency update was performed.
- Resolved Go helpers, types and new handler tests parsed with gofmt. The eight
  resolved conflict paths passed staged whitespace checks. Ent regeneration and
  backend policy tests are pending the rest of the merge, not claimed passing.

After D7 integration:

- Group-pricing round-trip regressions reproduced missing 1h/max-multiplier
  fields: 3 failed / 2 passed (21:07:42 local), then passed after helper changes.
- `COREPACK_ENABLE_AUTO_PIN=0 pnpm --dir frontend exec vitest run src/views/admin/__tests__/groupsModelPricing.spec.ts src/views/admin/__tests__/groupsOpenAIFast.spec.ts src/components/admin/group/__tests__/CodexManifestAccountsField.spec.ts src/views/admin/__tests__/groupsReasoningEffort.spec.ts src/components/admin/group/__tests__/ReasoningEffortPolicyFields.spec.ts`:
  exit 0, 33 tests, 1.92s (21:09:43 local). Cached Browserslist warning only.
- `go test -tags=unit ./internal/domain -count=1 -timeout=3m` from backend:
  exit 0, 0.002s, preserving model mapping contracts.
- `COREPACK_ENABLE_AUTO_PIN=0 pnpm exec vitest run src/composables/__tests__/useModelWhitelist.spec.ts`:
  18 tests passed, 901ms (21:11:18 local). The subsequent ad-hoc SFC check in that
  command failed to import a non-direct dependency, not a test failure.
- SFC script/template compilation rerun using the installed `vue/compiler-sfc`
  export passed. It initially exposed a const-reactive v-model warning; after
  replacing it with in-place Object.assign, the check passed without warnings.
  This is component syntax compilation only, not application typecheck/build.
- gofmt and staged whitespace checks passed for this checkpoint. No backend
  handler/service execution, ent regeneration, commit or push claimed yet.

After D8 integration:

- The initial ent generation command was blocked by conflict markers in generated
  group/mutation files. Their field switches were reconciled with the combined
  schema, and runtime descriptor indexes were matched to actual schema fields
  before regenerating. The official ent generator then exited 0. No go.mod or
  go.sum changes resulted. One concurrent git-diff read of generated files exited
  135; its output was discarded and the read rerun successfully after generation.
- `go generate ./ent` from backend exited 0 on a subsequent exclusive run.
  The tracked ent-file SHA-256 manifest digest before/after was identical:
  `d396dc93502f9ec024021e964df76386ff50e174db3617953c7168c45d2acf0f`.
  This checks ent repeatability only; Wire/full make check-generate remains pending.
- `go test -count=1 ./ent/...` exited 0: ent 0.005s, migrate 0.005s,
  schema 1.746s. The new runtime-default regression asserts KIRO defaults,
  disabled Fast/manifest switches, empty reasoning ceiling and downgrade action
  together, detecting field-index drift. No database was accessed.
- Service regressions added/extended this checkpoint are unexecuted because
  other service/handler files still contain merge conflicts. They are not claimed
  as red/green TDD evidence or end-to-end validation. No commit or push occurred.

After D9 integration:

- The five load-aware KIRO/channel cases were added before the narrow recovery
  exclusion change. Existing upstream load-aware route/sticky/wait tests remain.
- Ops field conflicts preserve local requested/mapped/KIRO model diagnostics
  alongside upstream credential-free proxy attribution and bounded attempt
  history. The queue test now checks model metadata survives both older and
  newest event windows, with truncation and existing redaction checks retained.
- Auth snapshot tests retain OpenAI (not Composite), include the approved deny
  field round trip, remove a duplicate version assertion, and check both prior
  local v21 and upstream v23 entries are evicted, alongside legacy v16.
- `go test -tags=unit ./internal/service -run 'Test(LoadAwareKiroRecoveryRespectsChannelAdmission|APIKeyAuthSnapshotOldVersionEvicted|SanitizeOpsUpstreamErrorsForQueueBoundsAndRedacts)$' -count=1 -timeout=3m`
  exited 1 during setup: unresolved marker in admin_service_group_test.go:963.
  No test bodies executed; this is neither behavioral red evidence nor a pass.
- gofmt parsed the seven changed Go files. Full backend execution, whole-merge
  review, commit and push remain pending.

After D10 integration (September 6 continuation):

- The approved legacy scheduling changes and new regression matrices parse with
  gofmt. `go test -tags=unit ./internal/service -run 'Test(LegacySelectionEnforcesChannelRestrictionAcrossPriorityPaths|LoadAwareKiroRecoveryRespectsChannelAdmission)$' -count=1 -timeout=3m`
  exited 1 during compilation on remaining conflicts in openai_gateway_grok.go,
  openai_gateway_messages.go and openai_gateway_response_handling.go. No test
  bodies executed; no behavioral red/green or passing claim.
- Embeddings, native-Anthropic-to-CC/Responses and Gemini-to-CC conflict results
  carry upstream headers while retaining local terminal observation, client
  disconnect and capture finalization. Native buffered partial-error result
  closures also carry the headers, not just successful results. The native
  Messages header reconciliation was held pending D11 at this checkpoint; the
  subsequent explicit user approval is recorded above.
- Admin group test conflicts retain the approved OpenAI Fast and pinned-manifest
  cases, without restoring excluded Composite/Live product tests. Pricing tests
  retain local Sol corrections and upstream Astra tier/long-context/alias cases
  as separate functions, matching the already-agreed pricing implementation.
- Whitespace checks passed for the checkpoint paths. No production access,
  commit, push or remote-pin expansion occurred.
