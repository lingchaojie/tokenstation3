# 2026-08-20 Wei-Shaw/sub2api upstream sync

## 固定范围

- 本地基线：`e2e9b307b0296d00cf4ce7b24b461acfee3ff91d`
- 合并基点：`0b3fe95afd20aba77ee7649b37febb8255fb57a5`
- 上游目标：`upstream/main` = `32a0d9ba2d537875f605e0360c28c7f8d418a29a` (`v0.1.178-52-g32a0d9ba2`)
- 回滚分支：`backup/dev-before-upstream-sync-20260820-020132`

## 取舍

同步通用上游能力，包括国产供应商、渠道监控、模型/时间定价、Responses
客户端工具适配、Codex/WS 增强和相关生成代码。KIRO 继续保留本地实现；KIRO
参考仓库不属于本次 Wei-Shaw 合并范围。

按本地产品边界保留删除项：Composite 产品实现及文档、Grok Voice/TTS/STT/
Realtime/custom voices/audio billing、独立 web-search surface、统一设置实现，
以及 prompt/security audit 的本地测试取舍。

本轮明确撤销本地引入的可能导致兼容性失败的强协议校验：OpenAI Responses SSE
跨事件生命周期/终端 envelope 校验、Anthropic/Gemini provider stream 严格顺序与
重复/容量校验、Chat/Responses 转换生命周期硬失败和 Bedrock terminal JSON 强校验。
仍保留 JSON 解码、响应转换、usage 提取、capture 边界和真实传输错误处理。计费
链路保留实际 usage 与模型价格回退，不因这些协议硬校验失败而把请求误记为断流或
静默 `$0`。

## 验证

- `go generate ./ent`
- `go generate ./cmd/server`
- `go build ./internal/service ./internal/handler ./internal/server/routes ./internal/repository ./cmd/server`
- 专项验证：Responses SSE 转 JSON、opaque numeric namespace payload、capture/usage
  相关路径通过。

完整历史测试集包含上游新增的“必须严格拒绝 malformed/reordered/duplicate stream”
断言；这些断言与本轮用户确认的“回到 sub2api 上游一致、移除本地强校验”目标冲突，
因此不作为本轮回退目标。发布前仍需在目标环境用真实 provider 代理链路验证断流和
usage billing。

本次只生成本地同步候选分支，未部署正式服、未推送远端。
